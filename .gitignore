# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local QA fixtures (third-party test FBX/PNGs) — not part of the app
test-assets/

# Local filesystem fallback for the published-snapshot storage backend (api/_lib/storage.ts) —
# used only when BLOB_READ_WRITE_TOKEN isn't set (plain local dev). Real deployments use Vercel
# Blob and never write here.
.data/

# Vercel CLI project link + pulled env vars
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
